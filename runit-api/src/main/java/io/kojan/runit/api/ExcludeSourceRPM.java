package io.kojan.runit.api;

/**
 * When put on RPM package test template method annotated with
 * {@link PackageTest} it makes test template exclude source packages.
 */
public @interface ExcludeSourceRPM {
}
